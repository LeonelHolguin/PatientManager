@model List<PatientManager.Core.Application.ViewModels.Medics.MedicViewModel>;
@using WebApp.PatientManager.Constants;
@{
    ViewData["Title"] = "Medic";

}

<h1>Mantenimiento Médicos</h1>

<div class="container-fluid">

    <div class="row mb-4">
        <div class="col-3 offset-9">
            <a asp-controller="Medic" asp-action="Create" class="btn btn-success">Nuevo Medico</a>
        </div>
    </div>

    <div class="row">

        @if (Model == null || Model.Count == 0)
        {
            <h2>
                No hay nada para mostrar
            </h2>
        }
        else
        {
    <table class="table align-middle mb-0 bg-white ">
        <thead class="bg-light">
            <tr>
                <th class="fw-bold" scope="col">Nombre</th>
                <th class="fw-bold">Correo</th>
                <th class="fw-bold">Telefono</th>
                <th class="fw-bold">Cédula</th>
                <th class="text-center fw-bold">Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        <div class="d-flex align-items-center">
                            <img src="@item.Photo"
                                 alt="Team Member's Photo'"
                                 style="width: 65px; height: 65px; object-fit:cover;"
                                 class="rounded-circle" />

                            <div class="ms-3">
                                <p class="fw-normal mb-1">@($"{item.Name} {item.LastName}")</p>
                            </div>
                        </div>
                    </td>
                    <td>
                        <p class="fw-normal mb-0">@item.Email</p>
                    </td>
                    <td>
                        <p class="fw-normal mb-1">@item.Phone</p>
                    </td>
                    <td>
                        <p class="fw-normal mb-1">@item.IdentityCard</p>
                    </td>
                    <td class="text-center">
                        <a asp-controller="Medic" asp-action="Edit" asp-route-id="@item.Id" class="btn btn-info btn-sm btn-rounded">
                            Editar
                        </a>

                        <a asp-controller="Medic" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger btn-sm btn-rounded">
                            Eliminar
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>

        }


    </div>

</div>
